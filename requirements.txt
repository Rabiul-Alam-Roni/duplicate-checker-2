fastapi>=0.95.0
uvicorn[standard]>=0.22.0
pandas>=1.5.0
openpyxl>=3.0.10       # for Excel file support
python-multipart>=0.0.5  # for form-data (file uploads)
jinja2>=3.1.0          # templating (likely included with FastAPI, but ensure present)
